specVersion: "v1beta"
serviceId: "academic-portal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "academic-portal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
